import { makeStyles } from "@material-ui/core";
export default makeStyles((theme)=>({
    cart:{
        maxWidth : '600px',
        paddingTop : '20px',
        minWidth:'600px',
        maxHeight: '100%',
        backgroundColor:'#f0ffff',

    }
}));