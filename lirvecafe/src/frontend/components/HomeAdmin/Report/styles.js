import {makeStyles} from "@material-ui/core";

export default makeStyles((theme) => ({
    charts:{
        display:'flex',
        flexDirection : "row",
        justifyContent: 'space-between',
        paddingLeft:'80px',
        paddingRight:'80px',
    }
}));