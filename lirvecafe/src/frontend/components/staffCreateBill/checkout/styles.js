import {makeStyles} from "@material-ui/core";

export default makeStyles((theme) => ({
    checkout: {
        paddingLeft: '60px',
        display: 'flex',
        minHeight: '100vh',
    },
    checkoutForm: {
        flexGrow: 1,
    }
    
    
}));